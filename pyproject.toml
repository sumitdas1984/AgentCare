[project]
name = "agentcare"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "sqlalchemy>=2.0,<3.0",
    "bcrypt>=4.0,<5.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]
